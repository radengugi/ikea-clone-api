const productsRouter = require('./productsRouter')
const usersRouter = require('./usersRouter')
const transactionRouter = require('./transactionRouter')

module.exports = {
    productsRouter, usersRouter, transactionRouter
}